#include <stdio.h>
int main(){
    int n;
    int a[]
    printf("Nhap N = ");scanf(N);
    printf("Mang gom n phan tu: \n")
    for (int i=0;i,n;i++){
        printf("A[",i,"] = ");scanf(a[i]);
    }
    int max,int x=1;
    max=a[0];
    while (int x<n){
        if (max > a[x])
            x++;
        else {
            max=a[x];
            x++;
        }
    }
    printf("Phan tu lon nhat trong mang la: ",max);
}